<Window x:Class="PicMark.SaveOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="另存为" Width="820" Height="620"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Alibaba PuHuiTi 3.0, Alibaba PuHuiTi, Microsoft YaHei UI, Microsoft YaHei, SimHei, Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFF6F7FB"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFB8BDC7"/>
        <SolidColorBrush x:Key="FieldBrush" Color="#FF2D2D2D"/>
        <SolidColorBrush x:Key="FieldDisabledBrush" Color="#FF242424"/>
        <SolidColorBrush x:Key="BorderBrush1" Color="#FF494949"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF5265FF"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#FF3A3A3A"/>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FieldBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource FieldBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource FieldDisabledBrush}"/>
                    <Setter Property="Foreground" Value="#FF8F96A3"/>
                    <Setter Property="BorderBrush" Value="#FF3B3B3B"/>
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkComboItem" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF464646"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF5265FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF808794"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboToggleButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF6A6A6A"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource FieldDisabledBrush}"/>
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#FF3B3B3B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkCombo" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{StaticResource FieldBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Style="{StaticResource ComboToggleButton}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <ContentPresenter x:Name="ContentSite"
                                              Margin="10,0,32,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>

                            <Path x:Name="Arrow"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,11,0"
                                  Fill="{StaticResource TextPrimaryBrush}"
                                  Data="M 0 0 L 5 5 L 10 0 Z"/>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Grid Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#FF2F2F2F"
                                            BorderBrush="#FF5A5A5A"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#FF000000" BlurRadius="16" ShadowDepth="4" Opacity="0.35"/>
                                        </Border.Effect>
                                    </Border>
                                    <ScrollViewer Margin="1" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="34"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF8F96A3"/>
                                <Setter TargetName="ContentSite" Property="Opacity" Value="0.72"/>
                                <Setter TargetName="Arrow" Property="Fill" Value="#FF6F7680"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#FF464646"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF545454"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF3C3C3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Border Background="{StaticResource PanelBrush}" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#FF000000" BlurRadius="24" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="52"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <TextBlock Text="另存为"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               Margin="22,0,0,0"/>
                    <Button HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,16,0"
                            Width="30"
                            Height="30"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            FontSize="18"
                            Click="Cancel_Click"
                            Cursor="Hand">
                        <TextBlock Text="×" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </Grid>

                <Grid Grid.Row="1" Margin="20,0,20,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#FF252525" CornerRadius="6" ClipToBounds="True">
                        <Grid>
                            <Image x:Name="PreviewImage" Stretch="Uniform"/>
                            <Border VerticalAlignment="Bottom" Background="#B51F1F1F" Padding="12,9">
                                <TextBlock x:Name="PreviewInfoText" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="图片品质" Style="{StaticResource LabelText}"/>
                            <ComboBox x:Name="QualityCombo" Grid.Column="1" Style="{StaticResource DarkCombo}" SelectionChanged="AnyOptionChanged">
                                <ComboBoxItem Content="高清" Tag="95" IsSelected="True"/>
                                <ComboBoxItem Content="标准" Tag="82"/>
                                <ComboBoxItem Content="省空间" Tag="68"/>
                            </ComboBox>
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition Width="82"/>
                                <ColumnDefinition Width="28"/>
                                <ColumnDefinition Width="82"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="82"/>
                                <ColumnDefinition Width="38"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="调整尺寸" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="WidthBox" Grid.Column="1" Style="{StaticResource FieldBox}" TextChanged="SizeBox_TextChanged"/>
                            <TextBlock Grid.Column="2" Text="宽" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBox x:Name="HeightBox" Grid.Column="3" Style="{StaticResource FieldBox}" TextChanged="SizeBox_TextChanged"/>
                            <TextBlock Grid.Column="4"/>
                            <TextBox x:Name="PercentBox" Grid.Column="5" Style="{StaticResource FieldBox}" TextChanged="PercentBox_TextChanged"/>
                            <TextBlock Grid.Column="6" Text="%" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>

                        <StackPanel Grid.Row="2" Margin="0,0,0,14">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="目标体积" Style="{StaticResource LabelText}" Width="72"/>
                                <RadioButton x:Name="NoCompressRadio" Content="不压缩" IsChecked="True" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Checked="CompressionRadio_Checked"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Width="72"/>
                                <RadioButton x:Name="CompressRadio" Content="压缩至" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Checked="CompressionRadio_Checked"/>
                                <TextBox x:Name="TargetSizeBox" Style="{StaticResource FieldBox}" Width="145" Margin="12,0,6,0" IsEnabled="False" TextChanged="AnyTextChanged"/>
                                <ComboBox x:Name="TargetUnitCombo" Style="{StaticResource DarkCombo}" Width="70" IsEnabled="False">
                                    <ComboBoxItem Content="KB" IsSelected="True"/>
                                    <ComboBoxItem Content="MB"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Grid.Row="3" x:Name="EstimateText" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap"/>

                        <Grid Grid.Row="4" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="96"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="格式名称" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="NameBox" Grid.Column="1" Style="{StaticResource FieldBox}"/>
                            <ComboBox x:Name="ExtCombo" Grid.Column="2" Style="{StaticResource DarkCombo}" Margin="8,0,0,0" SelectionChanged="AnyOptionChanged">
                                <ComboBoxItem Content=".png"/>
                                <ComboBoxItem Content=".jpg"/>
                                <ComboBoxItem Content=".webp"/>
                                <ComboBoxItem Content=".bmp"/>
                            </ComboBox>
                        </Grid>

                        <Grid Grid.Row="5" Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="66"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="保存路径" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="PathBox" Grid.Column="1" Style="{StaticResource FieldBox}"/>
                            <Button Grid.Column="2" Style="{StaticResource DarkButton}" Margin="8,0,0,0" Padding="8,0" Click="BrowseFolder_Click">
                                <TextBlock Text="浏览" FontSize="13"/>
                            </Button>
                        </Grid>

                        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource PrimaryButton}" Click="Save_Click" MinWidth="104" Margin="0,0,8,0">保存副本</Button>
                            <Button Style="{StaticResource DarkButton}" Click="Cancel_Click" MinWidth="76">取消</Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
